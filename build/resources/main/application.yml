server:
  port: 8080
  servlet:
    context-path: /mineria
  error:
      include-message: always




spring:
  security:
    basic:
      enabled: true

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kinterview
    username: root
    password: milena10
    hikari:
      maximum-pool-size: 2
jpa:
  hibernate:
    ddl-auto: none
  properties:
    hibernate:
      dialect: org.hibernate.dialect.MySQLDialect
      naming-strategy: org.hibernate.cfg.EJB3NamingStrategy
  show-sql: true

